import { DatoToStringPipe } from './dato-to-string.pipe';

describe('DatoToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new DatoToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
